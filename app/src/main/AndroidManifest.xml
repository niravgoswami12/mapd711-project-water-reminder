<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="nirav.samir.esha.project.waterReminder">

    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <application
            android:allowBackup="true"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:icon="@mipmap/ic_launcher"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity android:name="nirav.samir.esha.project.waterReminder.WelcomeActivity"
                  android:screenOrientation="portrait">
        </activity>
        <activity
                android:name="nirav.samir.esha.project.waterReminder.WaterStatisticsActivity"
                android:theme="@style/MainTheme"
                android:screenOrientation="portrait">
        </activity>
        <activity android:name="nirav.samir.esha.project.waterReminder.UserInformationActivity"
                  android:screenOrientation="portrait">
        </activity>
        <activity
                android:name="nirav.samir.esha.project.waterReminder.MainActivity"
                android:theme="@style/MainTheme"
                android:screenOrientation="portrait"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <receiver android:name="nirav.samir.esha.project.waterReminder.NotificationReceiver"/>
        <receiver android:name="nirav.samir.esha.project.waterReminder.ReBootReceiver"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
    </application>

</manifest>